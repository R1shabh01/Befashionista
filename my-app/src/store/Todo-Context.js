import { createContext } from "react";
// we use context when we dont want to pass the props through to many components .
// context act like a common space for such props
export const TodoContext = createContext([]);
